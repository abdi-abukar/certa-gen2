import { tradersResponse } from '@certa/server/traders';
export const runtime = 'nodejs';
export const GET = tradersResponse;
export const POST = tradersResponse;
export const OPTIONS = tradersResponse;
